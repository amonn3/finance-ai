import { Button } from "@/app/_components/ui/button";

const Home = () => {
  return (
    <div className="flex h-screen w-screen flex-col items-center justify-center">
      <h1 className="text-red-500">Dashboard Page</h1>
      <Button>Clica no pae</Button>
    </div>
  );
};

export default Home;
